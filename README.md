# PhonX Server

One-command installer that sets up any Linux VPS as a PhonX bypass server.

```bash
curl -sL https://raw.githubusercontent.com/phonx-project/phonx-server/main/install.sh | sudo bash
```
